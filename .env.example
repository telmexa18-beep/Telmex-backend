# .env.example
# Copia este archivo como .env y completa los valores reales.
# No subas el archivo .env real al repositorio.

MONGODB_URI=
JWT_SECRET=
EMAIL_USER=
EMAIL_PASS=
BREVO_API_KEY=

# Email del admin permitido para login de admin
ADMIN_EMAIL=
# Email usado como sender en emails de recuperación
EMAIL_SENDER=
# Password por defecto para carga inicial de workers (solo para pruebas, cambiar en producción)
WORKER_DEFAULT_PASSWORD=
